Добро пожаловать на https://the-mushroom-that-killed-mario.github.io/


```markdown
# 📁 Структура проекта



src/
├── **index.html**
│   └─ Разметка страницы, подключение модулей и базовые стили.
│
├── **main.js**
│   └─ Точка входа: инициализация эффектов, связывает UI и ScreenEffect.
│
├── **effects/**
│   ├── **ScreenEffect.js** — координирует все эффекты (дирижёр высокого уровня)
│   ├── **VideoLayer.js** — управляет видео: play / pause / reset
│   ├── **FlashEffect.js** — управляет вспышкой (анимация `blast`)
│   ├── **ShakeEffect.js** — управляет тряской страницы (анимация `shake`)
│   └── **utils.js** — утилиты: `forceReflow()`, `prefersReducedMotion()`
│
└── **assets/**
├── **explosion_hd.mp4** — видео взрыва
└── **frame.jpg** — постер (кадр-заглушка до воспроизведения)

```

---

## Описание

| Файл / Папка | Назначение |
|---------------|------------|
| **index.html** | Основная страница, подключает `main.js` как модуль |
| **main.js** | Инициализация `ScreenEffect`, обработчики кнопок «Взрыв» и «Сброс» |
| **effects/** | Модули, реализующие отдельные эффекты и логику |
| **assets/** | Медиафайлы (видео, изображения) |

---
